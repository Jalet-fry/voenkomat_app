# Список задач (ToDo List) — voenkomat_app

## 🔴 Приоритет 1: Исправление ошибок компиляции (Блокирующие задачи)
- [ ] **MainWindow.h**: Добавить объявления указателей на виджеты (`m_layout`, `m_queriesBtn`, `m_tablesBtn`, `m_exportBtn`, `m_restoreBtn`, `m_restoreTableBtn`, `m_exitBtn`) в секцию `private`.
- [ ] **MainWindow.cpp**: Добавить `#include "ConfigManager.h"`.
- [ ] **RecordDialog.cpp**: Добавить `#include <QSqlRecord>`, чтобы исправить ошибку "incomplete type 'class QSqlRecord'".
- [ ] **DatabaseManager.cpp**: Дописать реализацию метода `addColumn` (сейчас файл обрезан в конце).
- [ ] **DatabaseManager.h/cpp**: Синхронизировать сигнатуры методов (типы параметров и возвращаемые значения), чтобы устранить ошибки `no declaration matches`.

## 🟡 Приоритет 2: Пользовательский интерфейс и Юзабилити (Лабораторная №2)
- [ ] **UI Visibility**: Исправить стили в `setupStyles()` (MainWindow и другие окна). Установить черный текст для кнопок и полей ввода, убрать схему "белое на голубом".
- [ ] **CUA Standard**:
    - [ ] Реализовать перехват `F10` для активации меню.
    - [ ] Внедрить глобальные горячие клавиши: `Ctrl+A`, `Ctrl+V`, `Ctrl+D`, `Ctrl+U`, `Ctrl+Q`, `Ctrl+S`, `Ctrl+B`, `Ctrl+E`.
    - [ ] Настроить `Tab Order` во всех диалоговых окнах (чтобы перемещение по полям шло логично).
- [ ] **Удаление по ID**: Создать отдельное диалоговое окно для удаления записи по вводу ID (вместо удаления через выбор строки).
- [ ] **Статичные формы**: Проверить `RecordDialog`, чтобы размер окна подбирался под количество полей без использования скроллбаров.

## 🟢 Приоритет 3: Серверная часть и NoSQL (Лабораторная №3)
- [ ] **Backend**: Убедиться, что эндпоинты `/api/backup` и `/api/restore` требуют проверку токена суперпользователя.
- [ ] **BerkeleyDB**: Написать скрипт `converter.py` для переноса данных из PostgreSQL в BerkeleyDB (формат Key: ID, Value: JSON).

## 🔵 Технический долг и Warnings
- [ ] **EditTableStructureDialog.cpp**: Удалить неиспользуемую переменную `isNullable` в методе `onOkClicked()`.
