# Как скопировать новый libpq.dll в папку debug

## Шаг 1: Найдите скачанный файл

Скачанный файл `libpq.dll` (722 KB) обычно находится в:
- `C:\Users\ВашеИмя\Downloads\libpq.dll`
- Или в папке, куда вы сохранили файл

## Шаг 2: Скопируйте файл

### Вариант A: Вручную (проще всего)

1. Откройте папку `C:\QT_projects\voenkomat_app\debug`
2. Найдите старый файл `libpq.dll` (341 KB)
3. **Удалите** старый файл (или переименуйте в `libpq.dll.old`)
4. Скопируйте новый файл `libpq.dll` (722 KB) в папку `debug`

### Вариант B: Через PowerShell (автоматически)

Запустите скрипт `copy_new_libpq.ps1` (я создам его ниже)

## Шаг 3: Проверьте

После копирования проверьте:
- Размер файла должен быть **722 KB** (или около 739,328 байт)
- Дата изменения должна быть **сегодняшней**

## Важно

⚠️ **НЕ копируйте файл в C:\Windows\SysWOW64!**
- Ваше приложение ищет `libpq.dll` в папке `debug` рядом с `voenkomat_app.exe`
- Копирование в системную папку может вызвать проблемы

